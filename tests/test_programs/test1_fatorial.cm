/* Programa que calcula o fatorial de um numero */

int fat(int n) {
    if (n <= 1)
        return 1;
    else
        return n * fat(n - 1);
}

void main(void) {
    int x;
    x = input();
    output(fat(x));
}