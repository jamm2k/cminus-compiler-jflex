/* Calcula o n-esimo numero de Fibonacci */

int fib(int n) {
    if (n <= 1)
        return n;
    else
        return fib(n-1) + fib(n-2);
}

void main(void) {
    int n;
    int i;
    n = 10;
    i = 0;
    while (i < n) {
        output(fib(i));
        i = i + 1;
    }
}